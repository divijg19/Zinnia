// Shim for backward compatibility — re-export the canonical TypeScript implementation.
// KEEP: runtime compatibility shim. Do not remove unless runtime imports are updated.
export * from "./utils.ts";
